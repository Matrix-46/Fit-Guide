# backend/requirements.txt

Flask>=2.0.0,<3.0.0
Flask-SQLAlchemy>=2.5.0,<3.1.0
Flask-Login>=0.5.0,<0.7.0
Flask-Bcrypt>=0.7.0,<1.1.0
Flask-CORS>=3.0.0,<4.0.0

pandas>=1.3.0,<2.0.0
numpy>=1.20.0,<1.25.0        # Often a dependency of pandas/scikit-learn
scikit-learn>=0.24.0,<1.2.0  # For potential ML models, label encoding etc.
# joblib>=1.0.0,<1.3.0         # For saving/loading scikit-learn models (if you train and save any)

# Build tools (needed for MediaPipe and other packages)
setuptools>=65.0.0
wheel>=0.38.0

# OpenCV and MediaPipe for pose detection
# Using headless version for cloud deployment (no GUI dependencies)
opencv-python-headless>=4.5.0,<4.10.0
mediapipe>=0.10.5




# For Google Calendar API (if you implement reminders)
# google-api-python-client>=2.0.0,<3.0.0
# google-auth-httplib2>=0.1.0,<0.2.0
# google-auth-oauthlib>=0.4.0,<0.6.0

# Werkzeug is a dependency of Flask, usually installed automatically.
# Jinja2 is a dependency of Flask, usually installed automatically.
# itsdangerous is a dependency of Flask, usually installed automatically.

# Production server
gunicorn>=20.1.0,<22.0.0

# Environment variable management
python-dotenv>=0.19.0,<2.0.0
